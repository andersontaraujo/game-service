server:
  port : 8081

spring:
  application:
    name: Game Service
  redis:
    host: localhost
    port: 6379

springdoc:
  api-docs:
    path: /api-docs

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG